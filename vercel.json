{
  "buildCommand": "cd web && BROWSERSLIST=defaults next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "web/.next",
  "env": {
    "BROWSERSLIST": "defaults"
  }
}